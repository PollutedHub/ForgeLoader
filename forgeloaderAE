

local QueueTeleport =
    queue_on_teleport
    or (syn and syn.queue_on_teleport)
    or (fluxus and fluxus.queue_on_teleport)

if QueueTeleport then
    QueueTeleport(
        'loadstring(game:HttpGet("https://api.luarmor.net/files/v3/loaders/ecfcccea43f60fa4c46009f854c06a52.lua"))()'
    )
    print("Forge Loader queued for next server hop")
else
    warn("Your exploit does not support queue_on_teleport")
end

-- Your debug loop (safe)
local start = tick()
while tick() - start < 2 do
    print("DONE")
    task.wait()
end
